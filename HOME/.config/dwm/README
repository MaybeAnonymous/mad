dwm - dynamic window manager
--- dwm is an extremely fast, small, and dynamic window manager for X.


> Requirements
In order to build dwm you need the Xlib header files.

> Installation
Enter the following command (use root if necessary):
    make clean install

> Configuration
>> Patching
    Edit patches.h
    Don't forget to check config.mk
>> General
    Most configuration can be found in config.h
